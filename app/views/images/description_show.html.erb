<p id="notice"><%= notice %></p>

<table border="1">
	<tr>
		<th>Name</th>
		<th>File Name</th>
	</tr>
	<% @images.each do |image| %>
	<tr>
		<td><%= image.name %></td>
		<td><%= image.avatar %></td>
	</tr>
	<% end %>

</table>

<%#= link_to 'Edit', edit_image_path(@image) %> 
<%= link_to 'Back', description_image_index_path %>
