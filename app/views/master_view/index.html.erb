<table border=1>
	<tr>
		<th>Name</th>
		<th>Description</th>
	</tr>

	<% @master.each do |m| %>
	<tr>
		<td><%= link_to m.name, master_secondaries_path(m) %></td>
		<td><%= m.description %></td>
	</tr>	
	<% end %>
</table>

<%= link_to "Create New Master", master_view_new_path %>